server.port=8080
logging.level.root=DEBUG
```

This makes Spring print everything. Restart the app and the console will now show much more detail ? look for any red lines or the word `ERROR`.

---

## Step 3 ? Check if port 8080 is already being used

Open a terminal (anywhere) and run:
```
netstat -ano | findstr :8080